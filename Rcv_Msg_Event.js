// Literally simplest Rcv MSG event example

if (event.text.getString() == "Message")
    
// JsMacros.runScript("Script.js")
// Chat.say("Response")
  
  // Example output 
  // (Single reactive line for each event listener)
  
 // Cheap way how to detect selected player messages
  
  if (event.text.getString() == "<Nickname> Message")
Chat.say("Reply")

// Note, it is way better to use arrays, functions and variables for Remote Control Script based on RCV MSG

  
